# NOVA MEME HUNTER DASHBOARD V1.1.0 — APEX EDGE

Upload `index.html` to the root of `NOVA-MEME-HUNTER-DASHBOARD` and deploy with GitHub Pages from `main / (root)`.

The dashboard adds APEX Edge Intelligence diagnostics and ranks signals by the new APEX score.
